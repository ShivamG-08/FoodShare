{
  "name": "foodshare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
